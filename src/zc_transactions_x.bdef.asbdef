projection implementation in class ZBP_C_TRANSACTIONS_X unique;
strict ( 2 );
use draft;
define behavior for ZC_TRANSACTIONS_X alias ZcTransactionsX
use etag

{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}